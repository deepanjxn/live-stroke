{
  "name": "live-stroke-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -F live-stroke build",
    "dev": "pnpm -F playground dev",
    "lint": "eslint . --ext .ts,.tsx",
    "format": "prettier --write \"**/*.{ts,tsx,json,md}\"",
    "typecheck": "pnpm -r run typecheck",
    "clean": "pnpm -r run clean"
  },
  "engines": {
    "node": ">=18"
  }
}
